{
  "buildCommand": "rm -rf node_modules && cd apps/dashboard && rm -rf node_modules && npm install --legacy-peer-deps && npm run build",
  "installCommand": "true",
  "outputDirectory": "apps/dashboard/.next"
}
