{
  "buildCommand": "cd apps/dashboard && npm run build",
  "installCommand": "npm install && cd apps/dashboard && rm -rf node_modules package-lock.json && npm install",
  "outputDirectory": "apps/dashboard/.next"
}
