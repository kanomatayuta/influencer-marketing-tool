{
  "installCommand": "npm install --legacy-peer-deps && npm install --legacy-peer-deps --save-dev && cd apps/dashboard && npm install --legacy-peer-deps && npm install --legacy-peer-deps --save-dev",
  "buildCommand": "cd apps/dashboard && npm run build",
  "outputDirectory": "apps/dashboard/.next"
}
