{
  "installCommand": "npm install --legacy-peer-deps && cd apps/dashboard && npm install --legacy-peer-deps",
  "buildCommand": "npm run build:dashboard",
  "outputDirectory": "apps/dashboard/.next"
}
