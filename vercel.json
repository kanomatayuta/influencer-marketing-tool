{
  "buildCommand": "cd apps/dashboard && rm -rf node_modules package-lock.json && npm ci --legacy-peer-deps && npm run build",
  "installCommand": "true",
  "outputDirectory": "apps/dashboard/.next"
}
